<!--index.wxml-->
<view class="container">
  <view>index</view>
  <block wx:for="{{dishes}}" wx:key="timeStamp" wx:for-item="dish">
    <text class="dish-item">{{index + 1}}. {{dish.name}}</text>
  </block>
  <button bindtap="submitOrder">test</button>
</view>
